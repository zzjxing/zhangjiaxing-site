---
import type { ImageMetadata } from 'astro';
import Picture from '@/components/ui/Picture.astro';

export interface Props {
  images: ImageMetadata[];
  title: string;
}

const { images, title } = Astro.props;
---

{
  images.length > 0 && (
    <section class="gallery" aria-labelledby="gallery-title">
      <div class="gallery__heading">
        <p class="gallery__eyebrow">Visual record</p>
        <h2 id="gallery-title">Inside the work</h2>
      </div>

      <div class="gallery__grid">
        {images.map((src, index) => (
          <figure
            class:list={[
              'gallery__item',
              { 'gallery__item--wide': index % 3 === 0 },
            ]}
          >
            <Picture
              src={src}
              alt={`${title} project view ${index + 1}`}
              widths={[400, 800, 1200]}
              sizes={
                index % 3 === 0 ? '100vw' : '(min-width: 640px) 50vw, 100vw'
              }
              class="gallery__image"
            />
            <figcaption>
              {String(index + 1).padStart(2, '0')} / {title}
            </figcaption>
          </figure>
        ))}
      </div>
    </section>
  )
}

<style>
  .gallery {
    margin-top: calc(var(--space-3xl) * 1.5);
  }

  .gallery__heading {
    display: grid;
    grid-template-columns: minmax(8rem, 0.35fr) 1fr;
    align-items: end;
    gap: var(--space-xl);
    margin-bottom: var(--space-xl);
    border-bottom: 1px solid var(--glass-border);
  }

  .gallery__heading h2 {
    margin-bottom: var(--space-md);
    font-size: var(--text-3xl);
  }

  .gallery__eyebrow {
    margin-bottom: var(--space-md);
    color: var(--color-accent);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .gallery__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-lg);
  }

  .gallery__item {
    min-width: 0;
    margin: 0;
  }

  .gallery__item--wide {
    grid-column: 1 / -1;
  }

  .gallery__item :global(.gallery__image) {
    width: 100%;
    aspect-ratio: 1;
    object-fit: cover;
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-xl);
    box-shadow: var(--glass-shadow);
    background: var(--color-surface);
  }

  .gallery__item--wide :global(.gallery__image) {
    aspect-ratio: 16 / 9;
  }

  figcaption {
    margin-top: var(--space-sm);
    color: var(--color-text-muted);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.03em;
  }

  @media (max-width: 640px) {
    .gallery__heading {
      display: block;
    }

    .gallery__grid {
      grid-template-columns: 1fr;
    }

    .gallery__item--wide {
      grid-column: auto;
    }
  }
</style>
